# Data Extraction Rules for Quantitative Outcomes

## Source
Cochrane Handbook for Systematic Reviews of Interventions, Chapter 11 — Collecting data; adapted for LLM-based extraction.

## Rules

<!-- TODO: Fill with data extraction rules for continuous and dichotomous outcomes.

Suggested content:
- Transcriptionist principle: copy numbers exactly as written; never compute or derive.
- Data type classification:
    mean_sd        → reported as Mean ± SD
    mean_se        → reported as Mean ± SE
    mean_95ci      → reported as Mean [95% CI lower, upper]
    median_iqr     → reported as Median [Q1, Q3] or Median ± IQR
    median_range   → reported as Median [min, max]
    events_total   → reported as n/N (events out of total)
    percentage_total → reported as % with denominator
    or_ci          → paper directly reports OR [95% CI]
    not_reported   → value not found in text or tables
- raw_text field: always paste the exact text snippet the value was read from.
- Arm identification: match arm names to arm_ids from DocumentMap.
- Timepoint identification: match timepoint labels to timepoint_ids from DocumentMap.
- Table priority: prefer table data over in-text values when both are present.
- If SD <= 0, mark val2 as extracted but flag for MathSandbox validation.
- Do NOT record IQR or range as SD — use the correct data_type field.
-->

# TODO
